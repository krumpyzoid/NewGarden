export default class ShoppingCartItem {
    constructor(username, price, user, variation) {
        this.quantity = username;
        this.price = price;
        this.user = user;
        this.variation = variation;
    }
}